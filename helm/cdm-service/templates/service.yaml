apiVersion: v1
kind: Service
metadata:
  name: {{ include "cdm-service.fullname" . }}
  labels:
    {{- include "cdm-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "cdm-service.selectorLabels" . | nindent 4 }}

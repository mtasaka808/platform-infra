# Copy to backend.tfvars and fill in real values.
# Run: terraform init -backend-config=backend.tfvars
bucket         = "your-org-terraform-state"
key            = "cdm/dev/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "your-org-terraform-locks"

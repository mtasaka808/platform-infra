# Copy to terraform.tfvars and fill in real values.
# NEVER commit terraform.tfvars — it's in .gitignore.
aws_region     = "us-east-1"
aws_account_id = "123456789012"
github_org     = "your-github-org"
github_repo    = "dsca-cdm-demo"
